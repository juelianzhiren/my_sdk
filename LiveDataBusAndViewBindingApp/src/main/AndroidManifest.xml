<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.derry.livedatabusandviewbinding">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LIveDataBusAndDataBinding">
        <activity android:name="com.derry.livedatabusandviewbinding.simple1.MainActivity2"></activity>

        <activity android:name="com.derry.livedatabusandviewbinding.livedata.LodActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".livedata.DerryActivity">

        </activity>
    </application>

</manifest>